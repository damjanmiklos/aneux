---
alwaysApply: true

These are how the environments are set up on the HPC. (not how it is here locally)

---

**Active Environment Roles**

* **`aneuxai_env` (Training & Inference):**
* **Path:** `~/aneuxai_env`
* **Stack:** PyTorch `2.11.0+cu130`, `pyg-lib 0.9.0`, `torch_geometric 2.8.0`, native-compiled `pytorch3d 0.7.9`, `numpy 2.4.6`, `vtk 9.6.1`, `pyvista 0.47.3`.
* **Activation:**
```bash
source ~/aneuxai_env/bin/activate

```




* **`hemomesh` (VMTK Preprocessing & Geometry Extraction):**
* **Path:** `~/.conda/envs/hemomesh`
* **Stack:** Conda-forge Python `3.11.8`, `vmtk 1.5.0`, `vtk 9.2.6`, `numpy 1.26.4` (locked to NumPy 1.x C-ABI), `pyvista 0.48.4`.
* **Activation:**
```bash
source $(conda info --base)/etc/profile.d/conda.sh
conda activate hemomesh

---


